{
  "name"    : "AR",
  "version" : "1.0.0",
  "main"    : "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . TOMi --ignore=electron-packager --overwrite --platform=linux --arch=armv7l  --prune=true --out=dist"
  },
  "devDependencies": {
    "electron": "1.6.11",
    "electron-packager": "^8.6.0",
    "electron-rebuild": "^1.6.0"
  }
}
